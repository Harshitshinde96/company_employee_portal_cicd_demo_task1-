version: 0.0
os: linux
files:
  - source: /index.html
    destination: /usr/share/nginx/html/
hooks:
  BeforeInstall:
    - location: cleanup.sh
      timeout: 300
      runas: root
  AfterInstall:
    - location: reload_nginx.sh
      timeout: 300
      runas: root
